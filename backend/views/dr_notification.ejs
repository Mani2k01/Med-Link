<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Doctor Dashboard - Medical Software</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
         :root {
            --brand: #009688;
            --brand-dark: #00796b;
            --nav-height: 60px;
            --footer-height: 60px;
            --card-bg: #ffffff;
            --border: #e0e0e0;
            --text-muted: #777;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #e0f2f1, #f8fdfc);
            color: #333;
            min-height: 100vh;
        }

        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: var(--nav-height);
            background: var(--brand);
            display: flex;
            align-items: center;
            padding: 0 24px;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.15);
            z-index: 1000;
        }
        
        .navbar i {
            font-size: 26px;
            color: #b2dfdb;
            margin-right: 12px;
        }
        
        .navbar h1 {
            font-size: 22px;
            font-weight: 600;
            color: #fff;
        }
        
        .left-icons {
            margin-left: auto;
            display: flex;
            align-items: center;
            gap: 18px;
            font-size: 24px;
        }
        
        .left-icons a {
            color: #e0f2f1;
            text-decoration: none;
            transition: transform 0.2s ease, color 0.2s ease;
        }
        
        .left-icons a:hover {
            color: #ffffff;
            transform: scale(1.1);
        }

        main {
            flex: 1 1 auto;
            overflow-y: auto;
            padding: 20px;
        }

        .notification-wrapper {
            max-width: 900px;
            margin: auto;
            padding: 20px;
        }

        .notification-card {
            background: var(--card-bg);
            border-radius: 14px;
            padding: 18px 20px;
            margin-bottom: 16px;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 20px;
            animation: slideUp 0.4s ease;
        }

        .notification-details p {
            margin: 6px 0;
            font-size: 14px;
            line-height: 1.4;
        }

        .notification-details b {
            color: var(--brand-dark);
        }

        .action-buttons {
            display: flex;
            gap: 10px;
        }

        .view-btn,
        .rej-btn {
            border: none;
            padding: 8px 14px;
            font-size: 13px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            min-width: 130px;
            transition: background 0.2s ease, transform 0.2s ease;
        }

        .view-btn {
            background: var(--brand);
            color: #fff;
        }

        .view-btn:hover {
            background: var(--brand-dark);
            transform: translateY(-1px);
        }

        .rej-btn {
            background: #e74c3c;
            color: #fff;
        }

        .rej-btn:hover {
            background: #c0392b;
            transform: translateY(-1px);
        }

        .appointment-reminder {
            background: #fff3e0;
            border-left: 6px solid #ff9800;
            border-radius: 12px;
            padding: 18px;
            margin-bottom: 25px;
            box-shadow: 0 4px 14px rgba(0, 0, 0, 0.08);
        }

        .appointment-reminder h2 {
            font-size: 18px;
            color: #e65100;
            margin-bottom: 12px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .appointment-item {
            padding: 10px 0;
            border-bottom: 1px dashed #ddd;
            font-size: 14px;
        }

        .appointment-item:last-child {
            border-bottom: none;
        }

        .empty {
            text-align: center;
            padding: 40px;
            color: var(--text-muted);
            font-size: 15px;
        }

        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            height: var(--footer-height);
            background: var(--brand);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #e0f2f1;
            font-size: 13px;
            box-shadow: 0 -3px 10px rgba(0, 0, 0, 0.12);
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 600px) {
            .notification-card {
                flex-direction: column;
                align-items: flex-start;
            }
            .action-buttons {
                width: 100%;
                justify-content: flex-end;
            }
            .navbar h1 {
                font-size: 18px;
            }
        }
    </style>
</head>

<body>

    <!-- Navbar -->
    <div class="navbar">
        <i class="fa-solid fa-stethoscope"></i>
        <h1>Notification</h1>
        <div class="left-icons">
            <a href="/redirect_profile"><i id="user" class="fa-regular fa-circle-user"></i></a>
            <a href="/back"><i id="exit" class="fa-solid fa-arrow-right-from-bracket"></i></a>
        </div>
    </div>

    <main>
        <br><br><br>

        <% if (appointment && appointment.length > 0) { %>
        <div class="appointment-reminder">
            <h2>⏰ Today’s Appointments</h2>

            <% appointment.forEach(a => { %>
            <div class="appointment-item">
                <p><b>Date:</b> <%= a.appointment_date %></p>
                <p><b>Time:</b> <%= a.appointment_time %></p>
                <p><b>Reason:</b> <%= a.Reason_for_visit %></p>
            </div>
            <% }) %>
        </div>
        <% } %>

        <div class="notification-wrapper">

            <% if (results && results.length > 0) { %>
            <form method="post" action="/update_notification">

                <% results.forEach(r => { %>
                <div class="notification-card">

                    <div class="notification-details">
                        <p><b>Appointment Date:</b> <%= r.appointment_date %></p>
                        <p><b>Time:</b> <%= r.appointment_time %></p>
                        <p><b>Reason:</b> <%= r.Reason_for_visit %></p>
                    </div>

                    <div class="action-buttons">
                        <input type="hidden" name="nid" value="<%= r.id %>">
                        <button class="view-btn" name="action" value="approve">Make Appointment</button>
                        <button class="rej-btn" name="action" value="reject">Reject</button>
                    </div>

                </div>
                <% }) %>

            </form>
            <% } else { %>
            <div class="empty">
                🔔 No notifications available
            </div>
            <% } %>

        </div>
    </main>

    <div class="footer">
        <p>© 2025 VEDSP Intentive Private Limited. All rights reserved.</p>
    </div>

    <script>
        const backendAlert = "<%= typeof js_alert !== 'undefined' ? js_alert : '' %>";
        if (backendAlert) {
            alert(backendAlert);
        }
    </script>

</body>
</html>
